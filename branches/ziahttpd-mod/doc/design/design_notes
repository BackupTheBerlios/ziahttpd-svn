[ * ] Concerning cgi script execution:

 -> the session should have a list of resources
in usage in order to maintain the pid of the associated
cgi script the client requested. + connections are
persistent by default, so session have to.