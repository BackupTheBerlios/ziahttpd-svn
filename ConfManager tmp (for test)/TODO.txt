Jeudi :
- Integrer la gestion des expressions et des headers a GetValues

- Creer une methode specifique pour gere une valeur particuliere d'une liste
  <var name="test" elem="10"> ou <var name="test" elem="i">
  la methode doit directement retourner la bonne valeur sous forme de string
  ou "" si elle echoue et integrer cette fonction a GetValues (faire un appel)

A faire pour les jours suivants :



- surcharger l'operateur [] (doit avoir le meme effet que les fonctions Get*())
- surcharger l'operateur =  (doit avoir le meme effet que les fonctions Set*())
- gerer aussi l'operateur + pour concatener deux vertors ou deux string


- gerer le env

- gerer les boucles

- voir pour les options (ligne de commande)

- gerer aussi la conf par defaut s'il n'y a pas de fichier

- enlever les variables non reconnues par le serveur (case sensitive sur les noms des variables)





- faire des test de robostesse car il y a beaucoup de pointeur non tester et il faut verifier qu'ils ne puissent pas etre NULL et a la limite voir si certain test ne sont pas inutiles




- revoir le tri des fonctions (plusieurs fichiers?)

- ne pas enregistrer les variables egale a ""

- tracer toutes les erreurs dans un fichier de log


- Faire en sorte que le server puisse demander une valeur par defaut en cas de valeur conrompue (dans ce cas reset la variable a sa valeur par defaut)
  Le serveur doit verifier les valeurs apres un (re)chargement?

- formatter le code (limiter les lignes a 80 caracteres)
- voir pour d'evantuelles optimisation (surtout sur les test inutile et les pointeurs (non) stocke)

Pour les variables d'environnement
#include "stringmanager.hh"
stringmanager::sring p;
p.split();
